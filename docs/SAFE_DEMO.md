# Safe Demo Mode

For public demonstrations and contributor verification, prefer zero-cost/local or mock providers and keep every paid/publishing lock disabled.

A safe demo must not require real OAuth tokens, messaging credentials, provider API keys, or a real YouTube account. Synthetic seed data and the local demo provider are the preferred verification path.
