# Testing

The repository should remain verifiable without live provider accounts. Unit and integration tests should prefer deterministic fixtures, mocks, or local-demo paths and must not require production API keys or OAuth credentials.
