# Change Policy

Favor focused, reviewable changes. Behavior changes should include tests, and changes that touch credentials, OAuth, paid provider calls, remote media ingestion, or publishing should include explicit safety verification.
