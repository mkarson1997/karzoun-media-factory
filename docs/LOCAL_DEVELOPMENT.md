# Local Development

Use `.env.example` as the variable-name template and keep the real `.env` file untracked. For ordinary development and public verification, prefer local or mock providers and leave cost-bearing and publishing switches disabled.
