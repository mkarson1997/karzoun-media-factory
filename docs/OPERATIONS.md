# Operations

Production operation is intentionally separate from repository publication. Store credentials in deployment secret stores, keep generated media and runtime databases outside Git, and verify the safety locks before enabling any cost-bearing or public-publishing path.
