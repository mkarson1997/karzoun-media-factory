# Dependency Policy

Keep runtime and development dependencies pinned through `package-lock.json`. Review dependency updates for security, licensing, and behavior changes before release. Provider SDK upgrades must not bypass safety locks or introduce credential logging.
