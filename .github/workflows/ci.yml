name: Sonar src diagnostic

on:
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  diagnostic:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - name: Diagnostic branch only
        run: echo "Temporary Sonar src-layer probe. Do not merge."
