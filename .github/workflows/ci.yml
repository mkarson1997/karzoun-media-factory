name: Sonar app diagnostic

on:
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  diagnostic:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - name: Diagnostic branch only
        run: echo "Temporary Sonar app-layer probe. Do not merge."
